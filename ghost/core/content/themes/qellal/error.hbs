<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{statusCode}} — {{@site.title}}</title>
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}">
    {{ghost_head}}
</head>
<body class="error-page">
    <main class="hero" style="padding-top:6rem">
        <p class="cta-kicker" style="color:var(--muted)">Error {{statusCode}}</p>
        <h1 style="font-size:3rem">{{message}}</h1>
        <p class="lede">Something went wrong reaching that page. Head back home to keep tracking tenders.</p>
        <div class="hero-cta">
            <a class="btn btn-primary" href="{{@site.url}}">Back to home</a>
        </div>
    </main>
    {{ghost_foot}}
</body>
</html>
